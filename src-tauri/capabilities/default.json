{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["clip-mark", "recording-status", "capture-note"],
  "permissions": [
    "core:default",
    "opener:default",
    "positioner:default",
    "store:default",
    "log:default",
    "process:default"
  ]
}
